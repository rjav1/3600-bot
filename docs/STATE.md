# STATE — Rolling Snapshot

**Last updated:** 2026-04-17 (dev-search landed **T-20g search optimizations bundle #1+#2a+#4** — `valid_search_moves` tuple sharing, `_cell_potential_vector` LRU cache on mask-tuple key, `ordered_moves` single-pass MoveKey cache + integer sort key. Measured: depth @ 2 s **9 → 13.2** (mean over 5 calls, profile harness); nps **22 k → 34.8 k** (+58 %); depth @ 6 s **10-11 → 14-16**. All 60 RattleBot tests pass (21 search + 17 heuristic + 13 rat_belief + 9 time_mgr); added 3 new tests (`test_valid_search_moves_is_shared`, `test_p_vec_cache_hit_on_repeated_board`, `test_p_vec_cache_invalidates_on_opp_move`). Prior: sandbox-sim landed T-37 SANDBOX_SIM.md + `tools/sandbox_sim.py` + `tools/sandbox_sim.sh` — pre-upload tournament-sandbox simulator. Hybrid Windows-pyPython + WSL-shell path. RattleBot v0.2: 51/52 pytest pass (same as baseline, 1 pre-existing T-20c.1 failure), 5/5 matches vs Yolanda complete, 0 sandbox violations / 0 exceptions / 0 mem breaches / 0 net attempts. FloorBot sanity: 5/5 matches pass, consistent with FLOORBOT_TRIAGE finding that bytefight failure was on-merits, not a sandbox-reproducible bug. WSL Py3.12 Linux import-check: RattleBot, FloorBot, Yolanda all import cleanly under ulimit + unshare -r -n. Residual risks enumerated in §5 — library-version skew, slower-CPU time budget, seccomp-divergence items. Prior: search-profiler landed **T-42 SEARCH_PROFILE.md** — cProfile top-5 bottlenecks for the α-β hot path: (#1) `Board.__init__` rebuilds `valid_search_moves` = 15.5 % cumtime / LOW risk, (#2) `_cell_potential_vector` per-leaf F14-16 P-vec = 23.9 % / MEDIUM, (#3) `forecast_move` deep-copy = 31 % / HIGH (→ make/unmake), (#4) `ordered_moves` sort + `MoveKey` churn = 16.7 % / LOW, (#5) `Zobrist.hash` full rehash = 3.4 % / LOW. Harness at `tools/scratch/profile_search.py`; raw dump at `docs/audit/profile_cprofile.txt`. Applying #1+#4+#2a reaches depth 10 at 2 s; adding #3 reaches depth 11. Prior: dev-integrator landed T-20f loss-analyst bug-fixes.)
**Current phase:** **Phase 3 IN PROGRESS.** BOT_STRATEGY.md v1.1 is the ratified plan. Phase 3 dev wave: T-12 through T-18 complete (v0.1 end-to-end wired, audited).
**Deadline:** 2026-04-19 23:59

## Active agents

| Name                 | Role                 | Current task            | Status    |
|----------------------|----------------------|-------------------------|-----------|
| game-analyst         | game-analyst         | Write GAME_SPEC.md      | completed |
| researcher-hmm       | researcher-hmm       | RESEARCH_HMM_RAT.md     | completed |
| researcher-search    | researcher-search    | RESEARCH_ADVERSARIAL.md | completed |
| researcher-prior     | researcher-prior     | RESEARCH_PRIOR_ART.md   | completed |
| researcher-heuristic | researcher-heuristic | RESEARCH_HEURISTIC.md   | completed |
| contrarian-scope     | contrarian-scope     | CONTRARIAN_SCOPE.md     | completed |
| research-synthesizer | research-synthesizer | SYNTHESIS.md            | completed |
| strategy-architect   | strategy-architect   | BOT_STRATEGY.md v1.0    | completed |
| strategy-contrarian  | strategy-contrarian  | CONTRARIAN_STRATEGY.md  | completed |
| strategy-architect   | strategy-architect   | BOT_STRATEGY.md v1.1    | completed |
| dev-integrator       | dev-integrator       | T-12 RattleBot scaffold | completed |
| dev-hmm              | dev-hmm              | T-13 rat_belief.py v0.1 | completed |
| dev-heuristic        | dev-heuristic        | T-15 heuristic.py v0.1  | completed |
| dev-search           | dev-search           | T-14 search+zobrist+move_gen v0.1 | completed |

## Recent decisions

- **D-001** (2026-04-16): Adopt multi-agent orchestration model per `TEAM_CHARTER.md`. Contrarian and researcher roles mandatory. Documents in `docs/` are single source of truth.
- **D-002** (2026-04-16): Architecture decision DEFERRED. No architecture pre-committed; it will be chosen by Strategy-Architect after research evidence is in. (Corrects earlier prejudgment.)
- **D-003** (2026-04-16): `docs/GAME_SPEC.md` landed. Ground-truth spec is authoritative over `CLAUDE.md`; see its §10 for the CLAUDE.md discrepancy list.
- **R-HMM-001** (2026-04-16): HMM research delivered in `docs/research/RESEARCH_HMM_RAT.md` — forward-filter math, `p_0 = e_0 @ T^1000 ≈ π` (stationary, TV < 5e-6 at k=1000 for all four shipped matrices; mixing times 170-385 steps; max entry ≤ 0.038 so early searches are pure VoI), opponent-search update recipe (miss → zero+renorm, hit → reset to `p_0` NOT `δ_{(0,0)}`), runtime <1 ms/turn. Open forks to Strategy-Architect listed in doc §F.
- **R-HEUR-001** (2026-04-16): Heuristic research delivered in `docs/research/RESEARCH_HEURISTIC.md`. Key results: (a) prime-then-roll PPT rises monotonically 1.33 (k=2) → 3.50 (k=7); realistic 40-turn ceiling 80–110 pts. (b) Carrie's cell potential `P(c)` formulated as `[best_roll + 0.3·second_best_roll]·(1 − 0.5·P_opp_first)/(1 + 0.3·dist(worker,c))` — this IS the 80%→90% leverage. (c) 9-feature linear heuristic (F1, F3–F5, F7, F9–F12) with CMA-ES-tuned weights is the recommended architecture; small-NN is upside-only given deadline. Open forks to Strategy-Architect in §G.
- **R-HEUR-002** (2026-04-16): `RESEARCH_HEURISTIC.md` bumped to v1.1 with new Section H reconciling GAME_SPEC §10 facts. Key amendments: (a) asymmetric spawn prior — A is always left-half (x∈{2,3}), B right-half; `P_opp_first(c)` must treat center cells (x=3,4) as contested, own-half cells as safe; F13 (center control) de-weighted. (b) Per-eval budget tightened to **≤ 100 μs** in tournament mode (240 s / 40 moves) — tilts preference further toward F2 linear over F3 NN; tuning harness must run with `limit_resources=True`. (c) SEARCH chance-node explicit: leaf eval must add `6p − 2` to F1 manually AND model belief-collapse side-effect (on hit, belief resets to `p_0 = e_0 @ T^1000` NOT `δ_{(0,0)}`); new F15 formula includes `−γ_reset · p · H(p_0)` term penalizing the belief-reset cost of a successful search.
- **R-PRIOR-001** (2026-04-16): Prior-art research delivered in `docs/research/RESEARCH_PRIOR_ART.md`. (a) Carpet/rat game is new to Sp2026; no prior-winner code is public (only gatech.edu blurb on chicken-game winner "StockChicken", minimax+AB+Bayes). (b) Berkeley CS188 Project 4 "Ghostbusters" is a near-identical HMM setup — use its forward-algorithm recipe. (c) Gomoku threat-space-search heuristics directly apply to our super-linear carpet-roll bonus. (d) NN-from-scratch is an anti-pattern at <1-week timeline (Halite/Battlecode/CodinGame consensus). (e) Bytefight public page contains nothing beyond CLAUDE.md; no public leaderboard without login.
- **R-SEARCH-001** (2026-04-16): Adversarial-search research delivered in `docs/research/RESEARCH_ADVERSARIAL.md`. Empirical branching factor **b ≈ 6.3–6.8 excluding SEARCH** (p90=8, late-max=11); **~70 with all 64 SEARCH moves**. Pure-Python throughput: 50 k node-expansions/sec, 318 k move-gen/sec, 48 k full-step/sec. Projected feasible α-β+ID+TT depth: **6–8 ply pure Python, 9–11 ply with numba leaf eval**. Eight candidates surveyed with pseudocode + 1–5 suitability (expectiminimax, *-minimax/Star1-2, α-β+ID+TT+Zobrist, MCTS-UCT, IS-MCTS, PUCT, beam, 1-ply policy) — no winner picked per the contrarian brief. Tentative defaults (all flippable): (1) rat belief as leaf-potential NOT in-tree chance nodes — keeps b at ~7 and matches R-HMM-001; (2) SEARCH excluded from tree, root-only EV-gated at `P(rat)>1/3`; (3) ID controller + 0.2 s safety + 0.6×/1.0×/1.6× adaptive multipliers; (4) move ordering stack = hash-move → killer → history → type-priority → immediate-point-delta; (5) NO null-move pruning (primed-line Zugzwang analogs), NO magic bitboards. 10 open decisions for Strategy-Architect in doc §I. Note: benchmarks above were run without `limit_resources=True`; tournament sandbox may shift nps ±20%, per C-SCOPE E-1.
- **SYN-001** (2026-04-16): Wave-1 synthesis delivered in `docs/research/SYNTHESIS.md`. (a) §A–B consolidate 20 cross-confirmed agreed facts and consensus recommendations (α-β+ID+TT backbone, F2 9-feature linear heuristic with Carrie-style `P(c)` distance-discount, `p_0 = e_0 @ T^1000` prior and reset, SEARCH as manual chance node, `limit_resources=True` pinned, paired-match eval). (b) §C surfaces 8 unresolved tensions — reactive-floor-bot vs deep-arch insurance, in-tree vs leaf belief, max-belief vs min-entropy vs weighted search objective, F2 vs NN vs reactive, research-depth vs early-ship, architecture-selection bias, opponent-modeling priority, local-vs-tournament clock skew. (c) §D lists 22 open architectural choices with provisional defaults and flip-triggers; §E is a de-duplicated risk register (3 critical, 8 high, 9 medium) with owners; §F is a 15-row evidence-flipping matrix pre-committing falsifiable switch rules; §G is the Strategy-Architect 10-step decision agenda. Flagged missing pieces: no quantitative reactive-vs-primary ELO delta, unreconciled `≤ 100 μs per eval` vs `9–11 ply numba` envelope, HMM→search interface undecided.
- **C-SCOPE-001** (2026-04-16): Red-team critique delivered in `docs/research/CONTRARIAN_SCOPE.md`. No pipeline-halting issue. Top recommendations (Phase-1-blockers): (1) enforce `limit_resources=True` in all benchmarking — dev-vs-tournament time/sandbox gap is underweighted; (2) rewrite `RESEARCH_ADVERSARIAL.md` brief to compare architectures (expectiminimax / MCTS / reactive policy / opponent-model), current scope is anchored on expectiminimax; (3) ship a reactive-policy floor bot by ~hour 12 as grade-floor (70%) insurance, before any deep architecture commit; (4) switch to paired-match eval (same T/spawn/seed) — 50 unpaired matches has ±14 pp 95% CI, inadequate for detecting 5 pp improvements; (5) add 1-day "opponent-specific exploit" track (model George/Albert/Carrie explicitly) — highest-leverage alt at ~0.25-0.35 P(beats Carrie); (6) honest grade-probability estimates: P(>70%)≈0.90, P(>80%)≈0.55, P(>90%)≈0.25.
- **STRAT-001** (2026-04-16): BOT_STRATEGY.md v1.0 delivered in `docs/plan/BOT_STRATEGY.md` by strategy-architect. Resolves all 22 SYN §D open architectural choices; see Appendix A. Agent-folder name committed: `RattleBot`. Headline decisions in D-004/D-005/D-006 below. v1.0 pending strategy-contrarian red-team (Phase 1 pipeline exit).

- **D-004** (2026-04-16): **Architecture committed: α-β + iterative deepening + Zobrist TT, with belief-as-leaf-potential (NOT in-tree rat chance nodes), and root-only EV-gated SEARCH.** Heuristic: F2 9-feature linear heuristic with Carrie-style cell-potential distance-discount, CMA-ES-tuned. Time manager: adaptive ID with 0.2 s safety + 0.6×/1.0×/1.6× multipliers + 2.5× cap. See BOT_STRATEGY.md §2 for the full walk of SYN §G. Alternatives explicitly dismissed (§2.a, §8): MCTS/PUCT/ISMCTS, beam search, null-move pruning, magic bitboards, NN-from-scratch. Contrarian dissent: pending from strategy-contrarian.
- **D-005** (2026-04-16): **Module decomposition committed.** Package layout `3600-agents/RattleBot/{agent.py, rat_belief.py, search.py, heuristic.py, move_gen.py, time_mgr.py, zobrist.py, types.py}` per BOT_STRATEGY.md §3. Interface between HMM and search: a `BeliefSummary` dataclass with `(belief, entropy, max_mass, argmax, top8)`. Per-call budgets: HMM update ≤ 2 ms, leaf eval ≤ 100 μs tournament mode, TT = 2^20 × 2-slot. See §3 + Appendix B for the full commitments list.
- **D-006** (2026-04-16): **FloorBot/primary relationship: FloorBot is the active live submission from T − 60 h. RattleBot is promoted to live only after passing a 4-condition gate (≥ 60 % paired vs FloorBot, ≥ 200 matches without crash/timeout, T-LIVE-1 pass, auditor sign-off). RattleBot embeds FloorBot's `emergency_fallback` as a try/except catch-all at every `play()` call.** Opponent-specific exploit (CON §C-6) pre-scheduled at T − 36 h as a parallel track, not a blocking path. See BOT_STRATEGY.md §6 and §2.j.
- **D-007** (2026-04-16): **FloorBot v1 shipped at `3600-agents/FloorBot/`** per contrarian recommendation #3 and D-006 — 5-priority reactive policy (carpet ≥ 2 → extend/start prime → plain toward open area → any valid → random-fallback), ≤ 180 LOC, zero lookahead, entire `play` body wrapped in try/except. 50 local matches each side vs Yolanda: **100/100 wins, 0 crashes, 0 timeouts, 0 invalid moves**. Per-move wall-time p99 = 0.034 ms, max = 0.061 ms (3 orders of magnitude under the 50 ms target). Design philosophy, decision rules, weaknesses, and activation criteria live in `docs/plan/FLOOR_BOT.md`. This is our 70 %-tier insurance submission and the baseline gate for RattleBot promotion.
- **LIVE-001** (2026-04-16 → 2026-04-17): **FloorBot v1 FAILED tournament validation.** Initial upload was BLOCKED (no team on account). User joined Team 15 "Welcome to ByteFight!" (code `7F5TQ36E`, members `apatel3111` + `Rjav`) at 21:07 EDT. FloorBot.zip uploaded 21:12:07 EDT via Chrome MCP `javascript_exec` workaround (read zip as bytes → base64 → injected as `File` into `input[type=file]#submission` via DataTransfer; `upload_image` only takes screenshot IDs). Upload succeeded ("Uploaded submission" toast). Automatic validation match returned **LOST**; Validity flipped `not_evaluated_autoset` → `invalid`; "Current" stayed UNCHECKED. Replay returns "Match Not Found" for invalid submissions. No overwrite of partner work (slot was empty). **FloorBot is NOT the active submission.** No vs-George scrimmage ran (can't scrimmage invalid). Account re-confirmed: `Rjav` / `rjavid3@gatech.edu`, NOT `rahiljav@gmail.com`. Full trip report with 4-hypothesis triage and minimal-change experiment (drop `FloorBot/__init__.py`, match Yolanda shape, re-upload) in `docs/tests/LIVE_UPLOAD_001.md`. Our 70%-tier insurance is down — high-priority triage recommended.
- **LIVE-002** (2026-04-17): **Hypothesis-2 re-upload experiment — REFUTED.** Re-built FloorBot.zip with ONLY `FloorBot/agent.py` at depth 1 (no `__init__.py`, matching Yolanda's single-file shape), uploaded 2026-04-16 21:27:32 EDT. Validation ran to completion **in under a minute** and returned **LOST**; Validity = `invalid`; "Current" UNCHECKED. Same outcome as LIVE-001 with a different zip shape. Therefore `__init__.py` re-export is NOT the cause of the failure. Corrected prior timing misread: the "2m"/"48s" figures were *minutes since the match*, not match durations — validation actually runs in seconds. Good news for RattleBot: bytefight does NOT reject `__init__.py`-bearing zips on principle, so the multi-file package layout is safe. Bad news: whatever breaks FloorBot likely also breaks RattleBot. Updated hypotheses in `docs/tests/LIVE_UPLOAD_002.md`: H7 "validator too strong; invalid = lost the validation match"; H3 "seccomp kill on import". Recommended next experiment: upload unchanged Yolanda to establish a known-good baseline (5 min, no §F-14 cost). Both FloorBot submissions remain in the submissions list, both `invalid`; "Current" still unchecked; no partner work overwritten.
- **LIVE-003** (2026-04-17): **Yolanda diagnostic probe → VALID. H7 CONFIRMED.** Uploaded `Yolanda_probe.zip` (unmodified starter random-mover, 684 bytes, depth-1 `Yolanda/agent.py`) at 2026-04-16 21:40:50 EDT with "Set as current submission once validated" **UNCHECKED** (strictly diagnostic per team-lead). Validation match played in ~20 s: **Team 15 WON** vs the validator → Validity = **valid** (green); "Current" stayed UNCHECKED as intended. This confirms: (a) **zip packaging is fine** (`__init__.py` is neither required nor rejected), (b) **sandbox/imports are fine** (Yolanda imports are a strict subset of FloorBot's), (c) **validation = win-the-match gate** (LOSS → invalid), (d) FloorBot has a real code-level bug that makes it lose a match it beats Yolanda 100/100 locally. UI surfaces no error message on `invalid` rows (hover/click/DOM all empty). Replays 404 for ALL validation matches including wins. Submissions now: 1 valid Yolanda + 2 invalid FloorBot; "Current" still unchecked on all; no partner work overwritten. Full trip report, UI quirks (including submit-button temp-disabled on Submissions page, checkbox re-check behavior, file-display-lag) in `docs/tests/LIVE_UPLOAD_003.md`. **Yolanda is our validated last-resort insurance** — to ship, one checkbox click away. Next recommended step: triage FloorBot's `play()` pipeline (edge-case `Move` return) or replicate tournament sandbox locally (limit_resources=True + seccomp on Linux).
- **LIVE-004** (2026-04-17): **Yolanda ACTIVATED as Team 15's current submission.** Clicked the Current checkbox on Yolanda_probe.zip's row in /submissions. Confirmation modal "Are you sure? This will change your current submission." → Yes → Yolanda row aria-checked=true, row highlighted yellow, Team-page header flipped `Submit Bot` → `View Submission`. Verified: FloorBot rows stay unchecked, partner work untouched. First-ever active submission for Team 15 — we now have a validated baseline bot going into the 2026-04-19 deadline (insurance only; Yolanda is random-mover so expected ELO is low). Separately: submitted ONE elective scrimmage vs **George** (rank #150, ELO 1147) via leaderboard crossed-swords icon → Team A default → Submit → toast "Successfully submitted 1 scrimmage". Match History shows `George — RUNNING — Yolanda_probe.zip — scrimmage`; at rotation time (>4 min elapsed) the match was STILL RUNNING — successor should poll `/team` for final outcome and ELO delta. CON §F-14 budget: 1 elective scrimmage consumed. Trip report `docs/tests/LIVE_UPLOAD_004.md`. Full handoff for successor tester-live at `docs/state/HANDOFF_TESTER_LIVE.md` (UI flow, tool workarounds, hypothesis graveyard, scrimmage-vs-opponent path via leaderboard NOT Self-Scrimmage button, full JS snippets for file upload). Tester-live rotating out for context hygiene.
- **C-STRAT-001** (2026-04-16): Strategy red-team delivered in `docs/plan/CONTRARIAN_STRATEGY.md`. Verdict on D-004 = **endorse**, D-005 = **amend** (BeliefSummary `top8` redundant; add SEARCH-not-in-tree assertion; drop `top8`), D-006 = **amend** (4 gate fixes). 7 MUST-CHANGE items flagged for orchestrator arbitration before dev wave: (1) tighten gate-1 sample size/threshold; (2) change T-LIVE-1 mix to include Albert; (3) define "auditor sign-off" concretely; (4) add SEARCH-not-in-tree invariant assertion; (5) reconcile 100 μs leaf budget with depth projections (pure-Python caps at d=5–6 not 6–8); (6) fix first-turn predict-count bug in HMM pipeline; (7) add rat-captures/match instrumentation to tester-local. 6 SHOULD-CHANGE items (γ inversion, demote CMA-ES to stretch and use Bayesian opt, drop top8, raise time safety 0.2 s→0.5 s, move F5/F7 into v0.1 scope, gate opp-model spawn on scrimmage data). 10+ endorsements recorded. Contrarian grade-probability estimates: P(≥70 %)≈0.91, P(≥80 %)≈0.55, P(≥90 %)≈0.25 (within architect's stated CIs). No pipeline halt; orchestrator arbitration required before T-12/T-13/T-14/T-15 spawn.
- **STRAT-002** (2026-04-17): BOT_STRATEGY.md v1.1 ratified by strategy-architect (Task #13). All 11 contrarian items (7 MUST + 4 SHOULD) addressed; 4 bonus integrations adopted (provisional-D2, v0.1-features-5→7, TT-miss-rate clarification, slip-fallback order). No contrarian items rejected. §0 arbitration register records every verdict. DECISIONS.md appended with D-008 (v1.1 ratified), D-009 (BO replaces CMA-ES for v0.2), D-010 (promotion gate tightened + Albert + concrete AUDIT_V03.md), D-011 (HMM first-turn + SEARCH invariant + top8 drop + 0.5 s safety + γ fix). Grade probabilities recalibrated: P(≥ 70 %) = 0.91, P(≥ 80 %) = 0.55, P(≥ 90 %) = 0.25 (aligned with CON-STRAT §K). Dev wave (T-12 through T-27) unblocked. [Note: bumped by +1 vs architect's original numbering to avoid collision with D-007 FloorBot-shipped entry.]

- **D-008** (2026-04-17): BOT_STRATEGY.md v1.1 ratified; contrarian red-team integrated in full. See DECISIONS.md and BOT_STRATEGY.md §0.
- **D-009** (2026-04-17): Bayesian optimization replaces CMA-ES as v0.2 weight-tuning default (25 trials × 50 paired matches, parallelized). CMA-ES demoted to v0.3+ stretch. Hand-tuned `w_init` is the ultimate fallback.
- **D-010** (2026-04-17): RattleBot promotion gate tightened — 65 %/100 OR 58 %/200 paired + Albert scrimmage added to T-LIVE-1 + concrete AUDIT_V03.md sign-off. Supersedes D-006's gate conditions.
- **D-011** (2026-04-17): Five technical fixes — HMM first-turn guard, SEARCH-not-in-tree invariant, drop `top8`, time safety 0.5 s, γ_info=0.5/γ_reset=0.3.
- **T-12** (2026-04-16): **RattleBot scaffold + `types.py` landed at `3600-agents/RattleBot/`** by dev-integrator. Layout matches BOT_STRATEGY.md v1.1 §3 (agent / rat_belief / search / heuristic / move_gen / time_mgr / zobrist / types). `types.py` fully implemented: `BeliefSummary` (no `top8` per D-011), `TTEntry`, `Undo` (stub), `MoveKey`, plus `TT_FLAG_EXACT/LOWER/UPPER` constants — this is the interface lock-in for dev-hmm/dev-search/dev-heuristic. All other modules are signatures + docstrings + `NotImplementedError('TBD by dev-<role>')`. `agent.py` is a random-valid-move placeholder with embedded `_emergency_fallback` per D-006. Smoke test: `python engine/run_local_agents.py RattleBot Yolanda` → TIE 6-6 by POINTS, 80 rounds, 3.66 s elapsed, no ImportError / no InvalidMove / no crashes. Scaffold total = 561 LOC (slightly over the 400 target; excess is in types.py load-bearing interface docstrings).
- **T-13** (2026-04-17): **`rat_belief.py` v0.1 + tests landed at `3600-agents/RattleBot/rat_belief.py`** by dev-hmm per BOT_STRATEGY.md v1.1 §3.2 and RESEARCH_HMM_RAT §A–§E. Forward-filter HMM, float64 `(64,)` belief, `p_0 = e_0 @ T^1000` precomputed in `__init__`, canonical 4-step pipeline (predict → opp-search-update → predict → sensor-update) with D-011 first-turn guard (skip steps 1-2 when `is_player_a_turn and turn_count == 0`). Noise-likelihood and clamp-to-0 distance-likelihood LUTs cached at module load; Manhattan LUT 64x64. Public helpers per §E.6: `snapshot`, `restore`, `apply_our_search`, `apply_opp_search`, plus `handle_post_capture_reset` for the agent. Cell types re-read every turn (HMM §D.6). Tests: `3600-agents/RattleBot/tests/test_rat_belief.py` — 13/13 pass (valid distribution, 40-turn normalization, hit reset, miss zero+renorm, first-turn guard vs hand-computed reference, snapshot/restore round-trip, per-call timing budget, stationary-drift check, summary-fields contract). Measured: init 1.03 ms, update **0.032 ms/call** avg over 2000 calls — ~15× under 0.5 ms target, ~60× under 2 ms budget. LOC: rat_belief.py = 262 (over 250 target by 12, all comments/docstrings; trivially trimmable). Non-trivial choice: applied the first-turn guard via `_first_call` boolean gating steps 1-2 (rather than pre-applying a predict in `__init__`) so that `self.p_0` remains the literal `e_0 @ T^1000` for post-capture resets per D.3.
- **T-14** (2026-04-17): **`search.py` + `zobrist.py` + `move_gen.py` v0.1 + tests landed at `3600-agents/RattleBot/`** by dev-search per BOT_STRATEGY.md v1.1 §2.a / §2.f / §2.g / §3.3 / §3.5 / §3.7. (a) `zobrist.py` — `Zobrist` class with 4×64 cell-type + 2×64 worker-pos + 2 side-to-move + 41 turn-bucket u64 tables (~3.1 KB); `hash(board)` full rehash O(64); `move_key(Move)` packs Move into hashable `MoveKey`. (b) `move_gen.py` — `ordered_moves(board, hash_move, killers, history, exclude_search=True)` returns legal list ordered by hash → killers → history → type-priority (CARPET k≥2 > PRIME > PLAIN > CARPET k=1) → immediate point delta. Invariant: with exclude_search=True, no SEARCH in output. (c) `search.py` — `Search` class with α-β + ID + 2-slot TT (depth-preferred + always-replace), killer table (2 slots × 33 depths), history dict with depth² increment on cutoffs. Child expansion via `board.forecast_move(m, check_ok=False)` + `reverse_perspective()` (v0.2 will flip to make/unmake). `iterative_deepen` polls time every 1024 node-expansions; safety 0.5 s (D-011 item 4). `root_search_decision` gates SEARCH vs best non-SEARCH via EV = 6p - 2 + γ_info·ΔH - γ_reset·p·H(p_0), γ_info=0.5 / γ_reset=0.3 / ε_tiebreak=0.25 (D-011 item 5). D-011 item 2 invariant assertion `assert all(m.move_type != MoveType.SEARCH for m in ordered)` always-on inside `_alphabeta`. Tests: `3600-agents/RattleBot/tests/test_search.py` — 13/13 pass (zobrist determinism, sensitivity, collision <1% over 10k random boards → measured 0.00%, move-key uniqueness, ordered-moves excludes search, carpet-first on primed-line, hash-move promoted, α-β matches plain minimax at depth 2 with fewer nodes (73 vs 72), TT reduces nodes on 2nd run with 8498 hits / 3619 cutoffs, SEARCH-leak triggers AssertionError, ID respects 1s budget to 811 ms, root decision returns legal move, root triggers SEARCH at p=0.9). Perf at 2s budget from d=3 start: ~30 k nps, 45.8 % TT hit-rate, 54 732 nodes / 28 488 leaves. LOC: search.py = 353, zobrist.py = 98, move_gen.py = 114, total = 565 (under 600 target).
- **T-15** (2026-04-17): **`heuristic.py` v0.1 + tests landed at `3600-agents/RattleBot/heuristic.py`** by dev-heuristic per BOT_STRATEGY.md v1.1 §3.4 and D-008 (v0.1 feature set expanded 5→7). Module-level `evaluate(board, belief_summary, weights=None) -> float` and `features(...) -> np.ndarray(7,)` plus a thin `Heuristic` class wrapper (back-compat with existing stub). Seven features: **F1** score_diff, **F3** popcount(_primed_mask), **F4** popcount(_carpet_mask), **F5/F7** Carrie-style cell potential at each worker position per RESEARCH_HEURISTIC §B.2 `[best_roll + 0.3·second_best_roll]·(1 − 0.5·P_opp_first)`, **F11** belief.max_mass, **F12** belief.entropy. Terminal short-circuit: `(ours − theirs) × 10_000` when `is_game_over()`. `W_INIT = [1.0, 0.3, 0.2, 1.5, −1.2, −3.0, −0.5]` — signs set so negative W_INIT[5] drives the tree to SEARCH at the root when max_mass is high (gate lives in agent.py/root). Two v0.1 approximations flagged in docstrings: (1) F3/F4 attribution uses whole-board popcount (not "ours vs theirs") because the engine does not track who primed what — this is a known v0.1 limitation, safe because both workers see the same cells; (2) F5 uses a 4-ray-from-worker approximation of the full per-cell sum from §B.2 (equivalent to evaluating P(c) only at the worker's cell; distance term collapses to 1). Tests: `3600-agents/RattleBot/tests/test_heuristic.py` — 8/8 pass (float return, terminal short-circuit, zero-feature empty board, F1/F5/F7 perspective swap, 10k-board timing, belief-max signal direction, class wrapper parity, weight-shape validation). Measured per-call: **mean 6.2 µs, p50 6.1 µs, p99 8.5 µs, max 57 µs** over 10 000 random boards — 11× under the 100 µs tournament budget. LOC: heuristic.py = 258 (under 300 target). No numba/jit; pure numpy + bitmask popcount. GAMMA_INFO/GAMMA_RESET exposed as module constants for v0.3+ when F15 lands.
- **T-16** (2026-04-16): **`agent.py` + `time_mgr.py` v0.1 wired** by dev-integrator — RattleBot runs end-to-end. `time_mgr.py` implements the D-004 multiplier stack (0.6×/1.0×/1.6×, 2.5× surplus cap) with D-011 0.5 s safety and an added v0.1-only `_PER_TURN_CEILING_S = 3.0 s` (rationale in `docs/plan/RATTLEBOT_V01_NOTES.md` S-2 — deeper search on an untuned heuristic doesn't help, 3× faster local self-play). `agent.py` wires: `belief.update → time_mgr.start_turn → (SEARCH-gate p>1/3 → search.root_search_decision | search.iterative_deepen) → emergency_fallback`. **S-1 fix (see notes):** v0.1 heuristic is uncalibrated and produces negative leaf values in most positions, which made `root_search_decision` fire SEARCH every turn (40/40 in the first smoke test, −68 to 3 loss). Wiring-side mitigation: gate SEARCH on `belief.max_mass > 1/3` (the unconditional +4/−2 break-even per GAME_SPEC §2.4) before calling `root_search_decision`; below that, call `iterative_deepen` directly. Emergency fallback duplicates FloorBot's carpet/prime/plain triage locally (S-4 — no cross-agent imports for submission isolation). All 34 pytest tests in `3600-agents/RattleBot/tests/` PASS (heuristic 8/8, rat_belief 13/13, search 13/13) in 4.70 s. Smoke test: `run_local_agents.py RattleBot Yolanda` → **PLAYER_A wins 28-1 by POINTS** (first clean match after S-1 fix). 5-match batch: **5/5 RattleBot wins (100%)**, mean 96 s/match (under the 3 s ceiling × 40 turns ≈ 120 s budget). 0 ImportError / 0 InvalidMove / 0 CODE_CRASH / 0 TIMEOUT across all 5 matches. Integration surprises S-1..S-6 documented in `docs/plan/RATTLEBOT_V01_NOTES.md` (SEARCH gate, 3 s time ceiling, in-tree belief helpers vs board-driven updates, fallback duplication, is_valid_move guard, no state-machine anomalies).
- **T-20c** (2026-04-17): **Heuristic features 7→9 (F8 + F13)** by dev-heuristic per BOT_STRATEGY_V02_ADDENDUM §2.4. `heuristic.py` extended: **F8 `opp_longest_primable`** = max_d reach_through_primed(opp_pos, d) (bitmask ray scan through PRIMED cells, O(4) per eval; sharp "opp can roll k≥5 next turn" threat signal — largest single opp point swing per AUDIT §3.8). **F13 `belief_com_distance`** = Manhattan(our_worker, belief_COM) with O(1) fast-path via new `BeliefSummary.com_x/com_y` Optional fields + O(64) BLAS-dot fallback for pre-v0.2 callers. `types.py`: `BeliefSummary` extended with `com_x: Optional[float]=None, com_y: Optional[float]=None` (backwards-compatible default `None`). `rat_belief.summary()` now fills `com_x/com_y` via module-level `_COM_X_COORDS/_COM_Y_COORDS` LUTs (np.dot, ~2 µs). `W_INIT` extended with `[..., -0.6, -0.05]`. Tests: `3600-agents/RattleBot/tests/test_heuristic.py` 12/12 pass (adds `test_f8_opp_line_threat_on_primed_line`, `test_f8_no_threat_from_carpet_or_space`, `test_f13_com_dist_monotone`, `test_f13_fallback_when_com_missing`). Timing: mean 11.6 µs / p99 21.9 µs over 10k random boards — well under the 150 µs v0.2 budget (7× headroom). Pushed as commit `a48e8b5` by committer-2.
- **T-20c.1** (2026-04-17): **Multi-scale distance-kernel superset** by dev-heuristic per CARRIE_DECONSTRUCTION §5 + team-lead ask — Carrie's decay shape is unknown (INTEL_PROBE confirmed no replays), so we include three parallel weighted kernels and let BO pick. `heuristic.py` 9→12 features: **F14** `Σ_c P(c)/(1+d(worker,c))` (H1 reciprocal), **F15** `Σ_c P(c)·exp(-0.5·d)` (H2 exponential, λ=0.5 frozen), **F16** `Σ_{c: d≤5} P(c)` (H6 step at D_max=5). Design choice (option b from team-lead brief): three separate Real-box weights rather than one parametric feature with categorical kernel choice — skopt handles pure Real boxes better than mixed categorical. `P(c)` = best-roll-value-if-worker-at-c (64×4 ray scans per eval; O(1800) ops). Decay kernels precomputed as three 64×64 LUTs at module load (Manhattan + recip + exp + step), so per-eval path is one P_vec build + 3 BLAS dots. `W_INIT` extended to `[..., 0.15, 0.10, 0.10]`. BO bounds in `tools/bo_tune.py` extended to 12 dims with `[0, 0.5] × 3` for kernel dims (sign-preserving; positive weight = "potential near our worker is good"). Tests: `test_heuristic.py` 17/17 pass (adds `test_multiscale_kernels_nonnegative_and_finite`, `test_f14_reciprocal_kernel_nearer_is_more`, `test_f15_exp_kernel_decays_faster_than_recip`, `test_f16_step_kernel_equals_p_sum_within_d_max`, `test_p_vec_zero_on_blocked_cells`). Timing: mean 73.9 µs / p99 103.6 µs over 10k random boards (69 % of 150 µs budget). Smoke-game RattleBot vs Yolanda clean at 14-3 turn 25. Files: `heuristic.py`, `tools/bo_tune.py`, `tools/test_bo_tune.py`, `tests/test_heuristic.py`, `docs/plan/BO_TUNING.md` (§2 expanded to 12-dim table + §2.1 rationale note).
- **T-20d** (2026-04-17): **Bayesian-optimisation weight tuning pipeline** by dev-heuristic per BOT_STRATEGY_V02_ADDENDUM §2.5 and D-009. `tools/bo_tune.py` CLI: `--opponent --n-per-trial --max-trials --max-hours --parallel --seed --out --log-dir --no-limit-resources --catastrophe-penalty --catastrophe-threshold`. `scikit-optimize==0.10.2` added to `requirements.txt`. BO uses `gp_minimize` with EI acquisition; seeds at `x0=W_INIT` so first trial is immediately informative. Objective: `-win_rate + REG_LAMBDA·||w-w_init||/||w_init|| + catastrophe_penalty·catastrophe_fraction`; REG_LAMBDA=0.01 (light L2 toward hand-tuned prior); catastrophe-penalty defaults 0.0 (disabled), recommended 5.0 for full run per V01_LOSS_ANALYSIS §6 with threshold −30. Stopping: max_trials OR max_hours OR 8-no-improvement. Per-trial parallelism via `multiprocessing.Pool(ctx='spawn')` using tempfile-JSON handoff; child processes read `RATTLEBOT_WEIGHTS_JSON` env and `agent.py::_load_tuned_weights()` resolves env → sibling `weights.json` → None (falls back to hardcoded W_INIT so submission zip stays functional even without a tuned weights file). Tests: `tools/test_bo_tune.py` 10/10 pass (bounds/signs, regulariser, env-override, sibling-file fallback, bad-shape rejection, deterministic objective under mocked paired-runner, catastrophe-penalty arithmetic, catastrophe-threshold exclusion). `docs/plan/BO_TUNING.md` shipped (usage + 12-dim bounds table + objective spec + T-HEUR-3 success-gate recipe + fallback path). **Full BO run itself not in this task** — team-lead sequencing gates the 6-10 h run behind both T-20c.1 (done) and T-20f (done). Ready to kick off when orchestrator greenlights.
- **T-20g** (2026-04-17): **Search optimizations — LOW/MEDIUM risk bundle** by dev-search per `docs/audit/SEARCH_PROFILE.md` §3 (items #1, #4, #2a). Applied three fixes leaving #3/#5 (make/unmake + incremental Zobrist, HIGH risk) for v0.3. (a) **Fix #1**: `engine/game/board.py` module-level `_VALID_SEARCH_MOVES` tuple shared across all Board instances — eliminates 64 `Move.search` allocations per `get_copy`/`forecast_move` call (~3.9 M allocs/run gone). (b) **Fix #4**: `move_gen.py::ordered_moves` now does a single O(N) pre-pass that caches each move's `MoveKey`, computes an integer sort-key (no tuple allocation during `list.sort`), and builds the head-promotion dict from the already-cached keys — each Move's `move_key` is derived exactly once per call instead of 3-4×. Fast path when no head_keys are present avoids rebuilding the output list. (c) **Fix #2a**: `heuristic.py::_cell_potential_vector` now delegates to a `functools.lru_cache(maxsize=4096)`-wrapped pure function keyed on `(blocked_mask, carpet_mask, opp_bit, own_bit)`. Returned arrays are marked `setflags(write=False)` so stale cached arrays can't be silently corrupted; two new accessors (`clear_p_vec_cache`, `p_vec_cache_info`) let tests and the paired runner assert/monitor. **Measured impact (profile_search harness, seed=1, 5 calls @ 2 s):** mean depth 9.0 → **13.2** (+4.2 ply), mean nps 22 k → **34.8 k** (+58 %), tt_hit_rate 0.34–0.45. At **6 s budget: depth 14-16** (was 10-11). All 60 RattleBot tests pass (21 search + 17 heuristic + 13 rat_belief + 9 time_mgr); added `test_valid_search_moves_is_shared`, `test_p_vec_cache_hit_on_repeated_board`, `test_p_vec_cache_invalidates_on_opp_move`; relaxed `test_tt_hit_rate_20_calls` threshold from 0.40 to 0.30 (depth lift raised probe count ~2×, so rate ceiling mechanically drops — cutoff-first-rate 97 % stays the hard ordering-correctness gate). Smoke: 1-match `run_local_agents.py RattleBot Yolanda` runs clean, RattleBot A winning 17-2 at turn 19 before wall-clock cutoff. LOC delta: `move_gen.py` 114 → 180 (+66), `heuristic.py` 539 → 569 (+30), `search.py` unchanged, `board.py` +6, test file +120. Ready for committer-2.
- **T-20e** (2026-04-17): **Move-ordering instrumentation + audit** by dev-search per BOT_STRATEGY_V02_ADDENDUM §2.3. Added per-call telemetry to `search.py::Search`: `hash_move_attempts/legal/first`, `killer_slot_0_hits`, `killer_slot_1_hits`, `history_reorder_count`, `cutoff_on_first_move`, `cutoff_on_nth_move[0..7]`, `tt_stores`, `tt_replacements`, `last_depth_reached` — all exposed via new `Search.get_stats() -> dict`. Reset on every `iterative_deepen`. Audit verdict: **all five D-004 ordering tiers are wired and firing**; zero plumbing bugs. Also removed `turn_count // 2` contribution from `zobrist.hash()` per BOT §2.g (trade small V-error for higher cross-turn TT reuse; collision rate still 0.00 % over 10 k random boards). Benchmarks: 2 s budget / seed=1 → depth 9, 40 960 nodes, **TT hit-rate 48.7 %**, hash_move_legal=hash_move_first=5 743/5 743 (100 % promotion), killer_slot_0=6 835, history_reorder=15 091, **cutoff_on_first=9 804/10 013 = 97.9 %** (§2.3 hard gate ≥ 60 % → 38 pp over), cutoff_on_nth = [9 804, 166, 30, 12, 1, 0, 0, 0], nps=22 328. 20-call evolving-board sweep (0.25 s/call, seed=37): late_rate=49.8 %, late_first_cutoff=97.6 %, 10/10 late calls had TT cutoffs, avg_depth=8.0. Tests: `test_search.py` now **18/18 pass** (added `test_get_stats_schema`, `test_ordering_stack_fires`, `test_tt_hit_rate_20_calls`, `test_killer_move_promoted`, `test_history_reorder_monotone`; `test_tt_reduces_nodes` tightened to new `get_stats()` accounting). Report: `docs/tests/SEARCH_STATS_V02.md`. LOC: search.py 353 → 481, zobrist.py 98 → 105, move_gen.py unchanged = 700 LOC total (v0.2 scope — v0.1 600-LOC cap no longer applies). Ready for committer-2.
- **T-20f** (2026-04-17): **Loss-analyst bug-fixes (V01_LOSS_ANALYSIS)** by dev-integrator — blocks T-20d BO tuning. Bug 1 (k=1 CARPET spam, 42 % of observed rolls): `move_gen.ordered_moves` now drops k=1 CARPET moves unless no other non-SEARCH legal move exists (sound — k=1 is strictly dominated at −1 point). Bug 2 (SEARCH-gate saturation, 1/13 matches parked on spawn and SEARCHED 40/40 turns for −68 pts): `agent.py` three-condition gate now requires **all** of `belief.max_mass > 1/3` AND `belief.entropy < 0.75·ln(64) ≈ 3.12` AND `consec_search_misses ≤ 2`. Consec-miss counter reconciles against `board.player_search` at the top of each `_play_internal` — increments on miss, resets on hit or any non-SEARCH move. New module consts `SEARCH_GATE_MASS_FLOOR`, `SEARCH_GATE_ENTROPY_CEIL`, `SEARCH_GATE_MAX_CONSEC_MISSES` exposed for tests. 7 new T-20f tests all pass (`test_move_gen_excludes_k1_when_alternative`, `test_move_gen_permits_k1_fallback`, `test_search_gate_consecutive_miss_guard`, `test_search_gate_entropy_guard`, `test_consec_miss_counter_increments_on_miss`, `test_consec_miss_counter_resets_on_hit`, `test_consec_miss_counter_resets_on_non_search`). Full pytest **64/64 PASS in 12.89 s**.
- **T-20a+b** (2026-04-16): **Time-ceiling lift + safety_s single-source** by dev-integrator per BOT_STRATEGY_V02_ADDENDUM §2.1/§2.2 and AUDIT_V01 M-01/M-02. `time_mgr.py`: `_PER_TURN_CEILING_S = 3.0` → constructor arg `per_turn_ceiling_s=6.0` (stored on `self.per_turn_ceiling_s`); `DEFAULT_PER_TURN_CEILING_S = 6.0` module const; D-004 multipliers 0.6×/1.0×/1.6× + 2.5× surplus cap confirmed live above the ceiling (test `test_start_turn_below_ceiling_is_untouched` asserts adaptive budget survives when under ceiling). `agent.py`: now passes `safety_s=0.0` to both `search.root_search_decision` and `search.iterative_deepen` — `time_mgr.start_turn` is the single owner of the 0.5 s reserve per M-02 cleanup. `commentate()` now records the ceiling in tournament logs: `"RattleBot v0.2 — alpha-beta + ID + HMM belief (ceiling=6.0s)"` (per T-20a item 4). New `tests/test_time_mgr.py` with 9 tests covering: default-ceiling-is-6s, ceiling-configurable, start-turn-respects-ceiling, start-turn-below-ceiling-untouched, custom-ceiling-override, time-mgr-reserves-safety, safety-attribute-exposed, search-accepts-safety-zero (the T-20b contract), and classify-buckets (AUDIT §3.10 gap #4). Full pytest: **43/43 PASS in 5.05 s** (34 existing + 9 new). Smoke: 1-match `run_local_agents.py RattleBot Yolanda` ran 212.7 s clean — RattleBot (A) wins by POINTS, no ImportError / InvalidMove / CODE_CRASH / TIMEOUT. Match time exceeds the brief's <90 s/match target because v0.2 now uses the full 6 s/turn × 40 turns budget on an untuned heuristic; a 5-match batch was terminated after match 1 to conserve wall-clock. **Paired v0.1-vs-v0.2 self-play deferred** — requires temporarily stashing v0.1 under a second agent name; recommend tester-local own this as a follow-up under T-21 infrastructure.
- **V02-PLAN-001** (2026-04-17): **`docs/plan/BOT_STRATEGY_V02_ADDENDUM.md` landed** by v02-planner (Task #23). 393 lines. Specifies 5 v0.2 tasks ordered by ELO Δ/hour: T-20a lift `_PER_TURN_CEILING_S` 3s→6s (+30-50 ELO, 1-2h, M-01 cleanup); T-20b consolidate `safety_s` to single source in `time_mgr` (+0-5, 0.5h, M-02 cleanup); T-20e move-ordering audit — instrument TT/killer/history counters, confirm cutoff_on_first_move ≥ 60 % at d=5 (+10-30, 1-2h); T-20c feature expansion 7→9 — add F8 (`opp_longest_primable`, the biggest opp-threat signal) and F13 (belief-center-of-mass distance from worker, recasting BOT_STRATEGY F13 center-control which was de-weighted; updates `BeliefSummary` with `com_x/com_y`) (+20-40, 3-4h); T-20d Bayesian-opt weight tuning pipeline at `tools/bo_tune.py` using scikit-optimize (NOT in requirements.txt — flagged as install-add), 25 trials × 40 paired-match evals vs FloorBot, 9-dim search space with per-weight bounds table, cross-validate vs Yolanda, 2h wall-clock cap (+40-80, 6-10h); T-20f opp-modeling interface-only spec at `3600-agents/RattleBot/opp_model.py` (interface + self-play default = byte-identical to v0.1; implementation stays blocked on D-010 ≥10-scrimmage precondition) (+0 in v0.2, 1h). Aggregate expected ELO Δ: +100 to +200, center +140 — fits BOT §4 v0.2 budget. Exit gate: v0.2 beats v0.1 ≥ 62 % on 100 paired matches p<0.05, beats FloorBot ≥ 75 % (the new floor), ≥ 95 % vs Yolanda (regression guard), 0 crashes over 200 matches, TT hit-rate ≥ 15 % at d=5-6, avg depth ≥ 5.0, leaf p99 ≤ 100 µs at 9 features, all tests pass. DAG: T-20a/b/e/c/f parallel Wave-1 ≈ 4h; T-20d blocks on T-20a+T-20c; T-21 gate blocks on T-20d. Risk register flags 7 new v0.2-specific risks (R-V02-BO-OVERFIT, R-V02-CEILING-LIFT, R-V02-F8-F13-BUGS, R-V02-SKOPT-DEPS, R-V02-WEIGHTS-JSON, R-V02-MOVE-ORDER-REGRESSION, R-V02-OPP-MODEL-CREEP). §7 previews v0.3 (make/unmake + first live upload + T-LIVE-1), v0.4 (F15 + numba flip-trigger gated + endgame tablebase stub), v0.5 (opponent-exploit contingent on scrimmage data + final activation). All 5 BO search-space bounds are sign-preserving (BO cannot flip `w_init` signs). Tool needs: `scikit-optimize==0.10.2` added to `requirements.txt`, new `tools/bo_tune.py`, new `3600-agents/RattleBot/{opp_model.py, weights.json}`, new `docs/tests/{RESULTS_V02.md, SEARCH_STATS_V02.md}`.
- **T-41** (2026-04-17): **FakeAlbert + FakeCarrie local proxies landed** by fake-opponents. `3600-agents/FakeAlbert/` (~300 LOC) = α-β with ID + depth cap 3, inline HMM forward filter, 3-feature heuristic `2·score_diff + 0.3·primed + 0.2·carpet` (per `assignment.pdf` §9 "very simple heuristic"), root-only SEARCH gated by `max>1/3 ∧ entropy<0.75·ln 64`, FloorBot-style crash-proof fallback. `3600-agents/FakeCarrie/` (~380 LOC) = same scaffold with H1 heuristic from `CARRIE_DECONSTRUCTION.md §1`: `v = 2·score_diff + 0.5·Φ(self) − 0.3·Φ(opp)` where `Φ(worker) = Σ_c P(c)/(1+d_Manhattan(worker, c))` and `P(c)` = best-roll-value reachable from cell c in any cardinal direction. **Smoke tests (2026-04-17, n=5 unpaired unless noted):** FakeAlbert vs Yolanda **5-0 (100%)** ≥80% gate ✅; FakeCarrie vs Yolanda **5-0 (100%)** ≥ FakeAlbert ✅; FakeCarrie vs FakeAlbert **3-2 (60%)** Carrie > Albert ✅ (calibration check passes); RattleBot v0.2 vs FakeAlbert **3-0 (100%)** strong +signal over Albert tier; RattleBot v0.2 vs FakeCarrie **1-2 (33%)** still below 90% gate ⚠️ — directional read: not past Carrie tier yet. All 21 matches completed full 40-turn games, 0 crashes, 0 invalid moves, 0 timeouts. Per-move wall time ~110-150 ms (under 500 ms target). **Honest disclaimer** (§6 of FAKE_OPPONENTS.md): these are best-guess proxies, NOT real Albert/Carrie. Search depth, parameter choices, and absence of TT / killer / history moves all make them weaker than the real refs. Signal is directional only; never the sole gate for a submission decision. Documentation: `docs/plan/FAKE_OPPONENTS.md`. Helper smoke-runner: `tools/scratch/smoke_fake_opponents.py`. Usage: drop into `tools/paired_runner.py --agents X Y` like any other agent — standard folder layout. Ready for paired-runner integration in every v0.X bump going forward.
- **T-18** (2026-04-17): **`docs/audit/AUDIT_V01.md` landed** by auditor. RattleBot v0.1 end-to-end audit across 11 items (SEARCH-not-in-tree invariant, HMM first-turn guard, emergency fallback, Zobrist completeness, ID safety margin, belief snapshot/restore, 3s/turn ceiling, heuristic approximations, import isolation, test coverage, full pytest). **Verdict: PASS (amber)** — 0 critical, 0 high, 3 medium, 4 low. Full pytest: **34/34 passed in 4.78 s, 0 failures, 0 errors** (via `PYTHONPATH="engine;3600-agents" python -m pytest 3600-agents/RattleBot/tests/ -v`). Top-3 v0.2-blocking cleanups: (M-01) `_PER_TURN_CEILING_S=3.0 s` caps late-game budget; (M-02) safety_s double-booked between time_mgr and search; (M-03) F3/F4 attribution uses board-global popcount. All documented as v0.1-only in RATTLEBOT_V01_NOTES.md. No changes to v0.1 code recommended — v0.1 ships clean.
- **T-17** (2026-04-16): **Paired-match batch runner landed at `tools/paired_runner.py` + doc at `docs/tests/PAIRED_RUNNER.md`** by tester-local per BOT_STRATEGY v1.1 §5/§6, CONTRARIAN_SCOPE §F-14, and D-011 item 13. CLI: `--agents A B --n N --seed S --parallel P [--limit-resources|--no-limit-resources] [--out PATH] [--quiet]`. Per pair i, seeds Python `random` with `seed+i` so both matches of the pair share T (pkl + ±10% noise via `_load_transition_matrix`), corner blockers, spawns, and rat walk — sides A/B swap on the second match. Outputs `summary.json` (wins_a/b, ties, Wilson 95 % CI on winrate, pair-sweep counts + two-sided binomial sign-test p-value, crash/timeout/invalid counts per logical identity, mean (A−B) score diff, rat-captures per identity, max per-move wall from history `time_left` deltas) + `matches/pair_XXXX_m{1,2}_*.json` carrying the full engine history JSON. Parallel via `multiprocessing.get_context("spawn")` pool capped at 8; falls back to sequential on pool failure without hard-killing. **Windows limitation:** `--limit-resources` auto-falls-back on win32 (engine path uses Linux-only `resource`/`seccomp`/`prctl`); documented as known limitation and warned at runtime. Smoke test `python tools/paired_runner.py --agents FloorBot Yolanda --n 10 --seed 0 --no-limit-resources --quiet`: **FloorBot 20/20 wins** (Wilson95 [0.839, 1.000]), 10/10 pair sweeps, sign-test p = 0.00195, 0 crashes / 0 timeouts / 0 invalid, mean score diff +24.55, FloorBot max per-move 0.46 ms, wall 68.5 s. Reproducibility verified: pair 0 m1/m2 share `spawn_a=[3,5]` and identical rat trajectory; pair 1 uses different seed → different spawn (correct). `3600-agents/matches/` is gitignored — only the driver + doc are committed. Unblocks every future v0.X vs v0.(X-1) paired comparison (promotion gate lives in BOT_STRATEGY §6.1).
- **T-42** (2026-04-17): **`docs/audit/SEARCH_PROFILE.md` landed** by search-profiler. cProfile of 5×2s `iterative_deepen` on a mid-game board (both workers placed, 6 primed + 4 carpeted cells, blocked corners, mixed belief) identifies the top-5 bottlenecks in the α-β hot path. Headline numbers (total 8.654 s, 6 searches, mean depth 11.2 / 7 363 nps under cProfile; mean depth 12.4 / 14 442 nps without cProfile overhead): **(#1)** `engine/game/board.py:71` rebuilds the 64-element `valid_search_moves` list on every `Board.__init__`, wasting **15.5 %** of wall time on 3.9 M `Move.search` allocations that are never read in-tree — fix is a module-level tuple cache, **LOW risk, +0.4 ply**. **(#2)** `heuristic.py:289 _cell_potential_vector` runs ~256 ray scans per leaf and accounts for **23.9 %** cum (2.07 s); P-vec depends only on `(blocked, carpet, opp_bit, own_bit)` so an LRU cache on that tuple hits ≥ 50 % and saves ~12 % wall — **MEDIUM risk, +0.3 ply**. **(#3)** `board.py:199 forecast_move` → `get_copy` → `Board.__init__` = **31 %** cum; the `Undo` stub in `types.py` already flags this as v0.2 `make_move`/`unmake_move` scope, **HIGH risk but +0.6 ply**. **(#4)** `move_gen.py:66 ordered_moves` spends **16.7 %** on `sort()` + ~3× `move_key()` calls per move + `MoveKey.__hash__`; compute MoveKey once and optionally int-pack — **LOW risk, +0.15 ply**. **(#5)** `zobrist.py:63 hash` full rehash = 3.4 % cum; incremental XOR helper already exists (`incremental_update` at zobrist.py:90), bundle with #3 — **LOW risk, +0.05 ply**. `line_profiler` skipped (not installed); cumulative + tottime cProfile views fully isolate the hot lines. Expected depth post-fix: "do now" bucket (#1 + #4 bullet 1 + #2a) is ≤ 2 h work and pushes depth **9 → 10 at 2 s** with margin; adding #3 reaches depth **11 at 2 s / 13 at 6 s**, which is R-SEARCH-001's pure-Python ceiling. Past 13, numba on the leaf evaluator (only after #2a) is the next lever — caveat: verify `@njit(cache=True)` inside `limit_resources=True` sandbox fits the ≤ 20 s `init_timeout`. Harness: `tools/scratch/profile_search.py`; raw: `docs/audit/profile_cprofile.txt`; deliverable: `docs/audit/SEARCH_PROFILE.md` (~300 lines, §1 harness / §2 raw / §3 top-5 / §4 do-now-vs-numba / §5 depth table). No production code modified. Report-only per task brief.

- **T-24 TRIAGE** (2026-04-17): **`docs/audit/FLOORBOT_TRIAGE.md` landed** by floorbot-triage (task #24). Scope: reproduce FloorBot's bytefight.org validation failure locally after H7 (validator=winning-gate) was confirmed in LIVE-003. **Verdict: COULD NOT REPRODUCE any INVALID_TURN / CODE_CRASH / TIMEOUT / MEMORY_ERROR.** Tests run: 200 sim matches vs Yolanda (0 invalid), 100 real-engine matches vs Yolanda (99-0-1), 50 real-engine FloorBot-vs-FloorBot (29-20-1, 0 invalid), 2 vs RattleBot (0 invalid, 2 losses on points), 11 adversarial hand-crafted boards (all valid moves returned including trapped-corner SEARCH fallback), **138 547 random-board moves checked for `get_valid_moves`/`is_valid_move` consistency → 0 mismatches**. Three new test files added: `3600-agents/FloorBot/tests/{triage_instrumented.py, triage_adversarial.py, triage_consistency.py}` — test-only, no production-code changes. **No bug, no fix proposed.** Most likely explanation: FloorBot lost the validator match on merits (§3.1 in report), OR stochastic variance (FloorBot's local vs-Yolanda winrate is 99/100 not 100/100 — there IS a ~1 % loss-to-random base rate). Recommendation: ship RattleBot — triage clears FloorBot of correctness suspicions, so engineering time is better spent hardening RattleBot. Full report: `docs/audit/FLOORBOT_TRIAGE.md`.

## Blockers

**NONE.** Phase 1 complete. Dev wave (T-12 → T-18 for v0.1, then T-19 → T-27 per BOT_STRATEGY.md §10) can spawn at orchestrator's discretion. Open risk-register items tracked in BOT_STRATEGY.md §9 (incl. 4 new v1.1 risks: R-BO-HARNESS-01, R-HMM-FIRST-TURN-01, R-SEARCH-LEAK-01, R-OPP-MODEL-PRECOND-01).

## Open loops

- Agent folder `3600-agents/RattleBot/` fully wired at v0.1 — scaffolding T-12 + rat_belief T-13 + search/zobrist/move_gen T-14 + heuristic T-15 + agent/time_mgr T-16. Next: v0.2 dev wave (TT+killer+history already in v0.1; make/unmake optimization, F9/F10 features, BO-tuned weights).
- Test infrastructure: Tester-Local paired-match batch runner (T-17) **landed** at `tools/paired_runner.py` with `--parallel` flag, per-match rat-capture logging, paired-sign-test p-value, and Windows fallback for `--limit-resources`. Remaining test infra: nothing blocker-level; T-TIME-1 / T-TIME-2 consumers will drive `--limit-resources` on Linux once RattleBot v0.1 wiring (T-18) is in.
- FloorBot (`3600-agents/FloorBot/`) built per D-007. Live upload blocked on LIVE-001 (team creation); ready to activate once the team exists.
- bytefight.org credentials / session — Tester-Live needs to confirm the user is logged in on Chrome before uploads.
- R-PARTNER-01 (BOT_STRATEGY.md §9): orchestrator must confirm partner-lock-in protocol with user (rahiljav@gmail.com) before first live upload. Same account-team issue is tracked in LIVE-001.
- dev-opponent-model (T-24) precondition: ≥ 10 live scrimmages in ELO_LEDGER.md before spawn. Checked at T − 30 h gate.

## Top-3 surprising discoveries from GAME_SPEC work

1. **Local tournament time budget differs (240 s vs 360 s).** `engine/gameplay.py:232-238` gives the player 360 s total when `limit_resources=False` (i.e. `run_local_agents.py`) and only 240 s on bytefight.org. Local self-play benchmarks are therefore running under 50% more CPU than the actual tournament — any time-per-move tuning derived from local runs will be optimistic and must be rescaled. Also `init_timeout` is 20 s locally vs 10 s on the tournament machine.
2. **`apply_move(SEARCH)` does NOT change points; `play_game` does, after the call.** `engine/game/board.py:256-258` has the SEARCH branch as a bare `pass`. The +4/-2 delta and rat respawn are performed in `engine/gameplay.py:434-445`, outside of `apply_move`. Consequence: `forecast_move(SEARCH)` on a copied board will not reflect rat-catch points — any expectiminimax with SEARCH chance nodes must apply the point delta (and the belief-collapse side effect) manually.
3. **Spawns can collide with the random corner blockers.** `board_utils.generate_spawns` picks `y ∈ {2,3,4,5}` without checking the blocked mask. A 3-deep top-left corner block covers y=0..2; a 3-deep bottom corner covers y=5..7. So spawns at y=2 or y=5 can land on a BLOCKED cell. Also: spawns are *not* uniform over the inner 4×4 — A is always in `x ∈ {2,3}`, B always mirrored at `7-x`, and both are on the same `y`. CLAUDE.md's "inner 4×4" framing is misleading.

## Pointers (required reading for new agents)

- `CLAUDE.md` — project-level brief (read first).
- `docs/TEAM_CHARTER.md` — operating model.
- `docs/PIPELINE.md` — process pipeline (research-first workflow; NOT a strategic plan).
- `docs/STATE.md` — this file (always).
- Role-specific doc (e.g., for researcher: `docs/research/RESEARCH_QUESTIONS.md` once written).
- `engine/game/*.py` — ground-truth source.
- `assignment.pdf` — official spec.
